declare namespace bind_anomaly_zone {
    namespace anomaly_zone_binder{
        let disabled: boolean;
        function refresh(s: any, from: any): void;
        function turn_off(s: any): void;
        function turn_on(s: any): void;
    }
}